项目上传说明（源码上传版，不上传 node_modules，不默认打包）

GitHub 仓库地址：
https://github.com/supokedehljs/anapai-jie-tu-.git

本地项目目录：
D:\OneDrive\cursor_workspace\SnapAI-jie-tu

固定上传目标：
- 远程仓库名：origin
- 远程地址：https://github.com/supokedehljs/anapai-jie-tu-.git
- 分支：main

默认理解：
- 用户说“上传这个版本”“帮我上传”“上传到 GitHub”时，默认指上传当前项目源码改动到 GitHub
- 不默认执行打包，不默认生成 exe
- 只有用户明确提到“打包”“构建安装包”“生成 exe”时，才执行打包
- 上传时不上传 node_modules
- node_modules 保持在 .gitignore 中，不要移除

当前规则：
- 这个项目以后默认上传当前项目里改过的、新增的、之前遗漏的内容到 GitHub
- 除 node_modules 外，其它项目文件按需一起提交并推送
- 如果 node_modules 意外进入待提交列表，应先处理 .gitignore 或取消暂存，再提交

仓库当前基础状态：
- 已初始化 git
- 已连接远程仓库 origin
- 已推送到 main 分支
- 之前曾因为尝试上传 node_modules 和 Electron 大文件导致推送超时，所以以后不要上传这些内容

以后更新代码后的标准上传步骤：
1. 打开终端，进入项目目录：
   D:\OneDrive\cursor_workspace\SnapAI-jie-tu
2. 查看当前状态：
   git status
3. 确认 node_modules 没有进入待提交列表：
   git status --short
4. 将除 node_modules 外的全部文件加入提交：
   git add .
5. 提交本次修改：
   git commit -m "写一句这次修改的说明"
6. 推送到 GitHub：
   git push origin main

推荐提交说明写法：
- "优化多个置顶图片功能"
- "修复贴图切换按钮样式"
- "更新 RunningHub 工作流配置"
- "上传大致完善版本"
- "同步项目文件到 GitHub"
- "优化图标 名字 版本"

常用检查命令：
查看当前远程仓库地址：
   git remote -v

查看当前分支：
   git branch --show-current

查看当前状态：
   git status

查看本次修改摘要：
   git diff --stat

查看是否有被忽略的 node_modules：
   git status --short --ignored

如果提交时报用户名或邮箱未配置，可执行：
   git config user.name "supokedehljs"
   git config user.email "supokedehljs@gmail.com"

当前仓库使用的信息：
- 用户名：supokedehljs
- 邮箱：supokedehljs@gmail.com

以后执行上传时的默认理解：
- 上传项目改动内容到 GitHub
- 不上传 node_modules
- 不默认打包
- 不把 node_modules 从 .gitignore 中移除
- 如果 node_modules 出现在待提交列表，应先处理 .gitignore 或取消暂存，再提交
